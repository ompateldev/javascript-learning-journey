// let n = 7;

// for (let i = 0; i <= 10; i++) {
//   let table = n * i;
//   console.log(`${n} * ${i} = ${table}`);
// }

// for (let index = 0; index <= 10; index++) {
//   const element = index;

//   if (element % 2 === 0) {
//     console.log(`${element} this number is divisible `);
//   }

//   console.log(element);
// }


for (let index = 0; index <=10 ; index++) {
    const element = index;

    if( element == 2){
        console.log(`${element} this number is detected`);
        continue
    }

    console.log(element);
    
    
}