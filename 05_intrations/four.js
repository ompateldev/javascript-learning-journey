const myObject = {
  js: "JavaScript",
  cpp: "C++",
  py: "pythone",
};

for (const key in myObject) {
  console.log(`${key} => ${myObject[key]}`);
}

const mynewarry = [100, 300, 444, 555];
for (const key in mynewarry) {
console.log(key);

}
